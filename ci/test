#!/bin/sh

set -e

stack --no-terminal build --system-ghc --flag beam-core:werror --flag beam-sqlite:werror --flag beam-postgres:werror --flag beam-migrate:werror
stack --no-terminal test --system-ghc --flag beam-core:werror --flag beam-sqlite:werror --flag beam-postgres:werror --flag beam-migrate:werror
