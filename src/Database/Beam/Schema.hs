
module Database.Beam.Schema
    ( module Database.Beam.Schema.Types
    , module Database.Beam.Schema.Relationships ) where

import Database.Beam.Schema.Types
import Database.Beam.Schema.Relationships